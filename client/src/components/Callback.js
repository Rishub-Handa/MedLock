import React, { Component } from 'react';

class Callback extends Component {
    render() {
        return (
            <div>
                <img src={''} alt="loading" />
            </div>
        )
    }
}

export default Callback;
