import { fetchAMT }
/**
 * Registers a new user. 
 * @param {Object} newUser - The new user being registered.
 * @param {String} newUser.name - The name of the new user.
 * @param {String} newUser.email - The email of the new user. 
 * @param {String} role - The role of the new user, either "Patient" or "Provider". 
 */
